#!/usr/bin/env bash
# Yaam Init — scan project README and detect setup info
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_PATH="${1:-.}"

exec python3 "$SCRIPT_DIR/yaam-server.py" --project-path "$PROJECT_PATH" --init
